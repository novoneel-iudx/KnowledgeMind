# KnowledgeMind - Python dependencies
# ------------------------------------
# Install:  pip install -r requirements.txt
# Then:     python -m spacy download en_core_web_sm

# -- LLM SDKs ----------------------------------------------------------------
groq

# -- Orchestration -----------------------------------------------------------
langgraph              # used by monitor/fsm.py

# -- Vector store / embeddings / RAG -----------------------------------------
chromadb
sentence-transformers
torch                  # required by sentence-transformers (CPU wheel is fine)
pypdf

# -- NLP / commitment extraction ---------------------------------------------
spacy
# Install the model separately:  python -m spacy download en_core_web_sm
# On Streamlit Cloud this is handled by setup.sh automatically.

# -- Web search --------------------------------------------------------------
tavily-python
duckduckgo-search

# -- Connectors: Google (Calendar, Gmail) ------------------------------------
# Only needed when KM_GOOGLE_MOCK=false (real Google OAuth).
# google-auth
# google-auth-oauthlib
# google-auth-httplib2
# google-api-python-client

# -- Connectors: Slack -------------------------------------------------------
# Only needed when a real Slack token is configured.
# slack-sdk

# -- Knowledge graph ---------------------------------------------------------
networkx
pyvis

# -- UI / config / utilities -------------------------------------------------
streamlit
python-dotenv
tabulate
